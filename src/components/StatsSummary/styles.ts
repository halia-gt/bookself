import styled from "styled-components";

const Wrapper = styled.section`
    margin-top: 3rem;
`;

const MainStatsWrapper = styled.table`
    width: 100%;
    margin-top: 1rem;
    
`;

export {
    Wrapper,
    MainStatsWrapper,
};