export type ReadingBook = {
    id: number,
    title: string,
    author: string,
    image: string,
    format: string,
};

export type NextReadsTBR = {
    id: number | undefined,
    image: string | undefined,
};
